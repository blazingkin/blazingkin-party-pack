<% id = 0 %>
<% sentences.each do |sentence| %>
    <div class="row">
        <br />
        <div class="btn <%= !winner.nil? && winner == id ? 'btn-primary' : 'btn-danger' %> sentence-vote-btn" data="<%= id %>"><%= sentence[:sentence] %></div>
    </div>
    <% id = id + 1 %>
<% end %>